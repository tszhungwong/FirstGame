# Concept art

Store source concept references here. They guide production art but are not runtime sprites, textures, or export dependencies.

The current repository-level `assets/` reference images predate this folder and are intentionally left in place. Copy approved, runtime-ready derivatives into `res://assets/runtime/` only.
