# Runtime assets

Only runtime-ready assets belong here. The initial graybox uses programmatic visuals in later tasks; concept art belongs in `res://source_art/concepts/` and remains separate from runtime assets.
